{
  "name": "student-feedback-app",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm:tailwind:build\" \"vite\"",
    "build": "vite build",
    "preview": "vite preview",
    "tailwind:build": "npx @tailwindcss/cli -i ./src/tailwind.css -o ./src/style.css --watch"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.1.11",
    "axios": "^1.6.0",
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "concurrently": "^9.2.0",
    "vite": "^5.0.0"
  }
}
